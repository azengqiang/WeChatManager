<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="pre.my.test.robot.mapper.SubscribeDetailMapper">
    <resultMap id="BaseResultMap" type="pre.my.test.robot.dto.user.SubscribeAnalysis">
        <id column="id" property="id"/>
        <result column="openid" property="openid"/>
        <result column="action" property="action"/>
        <result column="creationDate" property="creationDate"/>
    </resultMap>

    <select id="selectUserNum" parameterType="pre.my.test.robot.dto.user.SubscribeAnalysis" resultMap="BaseResultMap">
        select * from rb_subscribe
        <where>
            <if test="action !=null ">
                and action = #{action}
            </if>
            <if test="startDate !=null and startDate != ''">
                and  creationDate  <![CDATA[ >= ]]> #{startDate}
            </if>
            <if test="endDate !=null and endDate != ''">
                and  creationDate  <![CDATA[ <= ]]> #{endDate}
            </if>
        </where>
        group by openid
    </select>

    <select id="select" parameterType="pre.my.test.robot.dto.user.SubscribeAnalysis" resultMap="BaseResultMap">
        select * from rb_subscribe
        <where>
            <if test="action !=null ">
                and action = #{action}
            </if>
            <if test="startDate !=null and startDate != ''">
                and  creationDate  <![CDATA[ >= ]]> #{startDate}
            </if>
            <if test="endDate !=null and endDate != ''">
                and  creationDate  <![CDATA[ <= ]]> #{endDate}
            </if>
        </where>
    </select>


    <!--执行增加操作的SQL语句。id和parameterType分别与IUserInfoService接口中的save方法的名字和参数类型一致。
  useGeneratedKeys设置为"true"表明要MyBatis获取由数据库自动生成的主键；keyProperty="id"指定把获取到的主键值注入到User的id属性-->
    <insert id="save" parameterType="pre.my.test.robot.dto.user.SubscribeAnalysis"
            useGeneratedKeys="true" keyProperty="id">
        insert into rb_subscribe(openid,action,creationDate)
        values(#{openid},#{action},CURRENT_TIMESTAMP)
    </insert>

    <update id="update" parameterType="pre.my.test.robot.dto.user.SubscribeAnalysis">
        update rb_subscribe rs
        <set>
            <if test="action!=null">
                rs.action = #{action},
            </if>
        </set>
        where openid=#{openid}
    </update>

    <delete id="delete" parameterType="pre.my.test.robot.dto.user.SubscribeAnalysis">
        delete from rb_subscribe where openid=#{openid}
    </delete>
</mapper>